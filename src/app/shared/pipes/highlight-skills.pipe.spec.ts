import { HighlightSkillsPipe } from './highlight-skills.pipe';

describe('HighlightSkillsPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightSkillsPipe();
    expect(pipe).toBeTruthy();
  });
});
